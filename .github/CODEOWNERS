# CODEOWNERS for main (PROD)
# Temporarily aligned with qa/uat: release-approvers governs every branch
# for now, including main. The stricter prod-access-only requirement was
# reverted (2026-08-20) - re-introduce a narrower team here later if a
# distinct, smaller prod approval circle is wanted again. Note: keeping
# this identical to qa/uat's CODEOWNERS also removes the recurring merge
# conflict that a genuinely different main-only file was causing on every
# uat -> main promotion.

* @Dk-test-007/release-approvers
